[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.prod"

[deploy]
startCommand = "gunicorn config.wsgi:application --bind 0.0.0.0:8000"
preDeployCommand = "python manage.py migrate && python manage.py seed_plans"