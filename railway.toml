[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.prod"

[deploy]
preDeployCommand = "python manage.py migrate"
startCommand = "python manage.py seed_plans && gunicorn config.wsgi:application --bind 0.0.0.0:8000"